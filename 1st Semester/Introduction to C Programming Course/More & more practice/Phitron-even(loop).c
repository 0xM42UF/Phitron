#include<stdio.h>
int main()
{
    int n,m;
    scanf("%d",&n);
    scanf("%d",&m);

    for(int i=1; i<=n; i++)
    {
        if(i%2==0)
        {
            printf("%d ",i);
        }
        else if(i = m)
        {
            printf("%d",i);

        }
    }
    return 0;
}
